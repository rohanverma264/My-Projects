function keyCode(key) {
    var input = document.getElementById('screen');
    var result = document.getElementsByClassName('grid-items').innerHTML;
    input = result;
    input.innerHTML= result;
    
}